---
# maintains mainnet inventory grouped by project names
all:
  vars:
    env: mainnet
  children:

    avail:
      hosts:
        ts.validator.avail.mainnet.encapsulate.xyz:
          type: validator

    vara:
      hosts:
        ts.validator.vara.mainnet.encapsulate.xyz:
          type: validator
